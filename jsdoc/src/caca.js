/**
 * The function named "resta" takes two parameters, a and b, and returns the result of subtracting b
 * from a.
 * @param a - The function `resta(a, b)` takes two parameters `a` and `b`, and it calculates the
 * difference between `a` and `b` by subtracting `b` from `a`.
 * @param b - The parameter `b` in the `resta` function represents the second number that will be
 * subtracted from the first number `a`.
 * @returns The function `resta` is returning the result of subtracting `b` from `a`.
 */
function resta(a,b){
    return a-b;
}

/**
 * The function `div` takes two parameters `a` and `b` and returns the result of dividing `a` by `b`.
 * @param a - Thank you for providing the `div` function. How can I assist you with the parameters for
 * `a`?
 * @param b - It seems like you were about to provide some information about the parameter `b`, but the
 * message got cut off. Could you please provide more details or let me know how I can assist you
 * further?
 * @returns The function `div` is returning the result of dividing the first parameter `a` by the
 * second parameter `b`.
 */
function div(a,b){
    return a/b;
}