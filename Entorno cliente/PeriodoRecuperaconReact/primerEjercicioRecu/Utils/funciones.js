export function getFetch(url){
    return fetch(url)
            .then(resp => resp.json())
}

export function addCarrito(arr, obj, setCarr) {

    /* let esta = arr.find(p => p = obj) */

    /* setCarr(...arr, obj) */
}